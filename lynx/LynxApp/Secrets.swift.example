// Template for the git-ignored secrets file.
// Copy to LynxApp/App/Secrets.swift and fill in real values, then run `xcodegen`.
enum Secrets {
    /// Google AI Studio key (https://aistudio.google.com/apikey).
    static let geminiKey = "YOUR_GEMINI_KEY_HERE"
}
